<%= form_for(@tarefa) do |f| %>
  <% if @tarefa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tarefa.errors.count, "error") %> prohibited this tarefa from being saved:</h2>

      <ul>
      <% @tarefa.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %><br>
    <%= f.text_field :descricao %>
  </div>
  <div class="field">
    <%= f.label :acao %><br>
    <%= f.text_field :acao %>
  </div>
  <div class="field">
    <%= f.label :seletor %><br>
    <%= f.text_field :seletor %>
  </div>

  <div class="field">
    <%= f.label :conexao %><br>
    <%= f.select :conexao_id,  options_for_select( Conexao.all.collect{|a| [a.database,a.id]}, @tarefa.conexao_id )%>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
